#include "functions.h"
#include <iostream>

int main() // Main executes...ready to move in and live in the space... 
{
    Browser browser; // Create the browser instance
    browser.menu();  // Start the menu
    return 0; // Exit function
}
